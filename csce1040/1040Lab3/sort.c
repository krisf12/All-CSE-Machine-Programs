#include<stdio.h>
#include<stdlib.h>

void mergesort(int, int, int);
void merge();

int main(void)
{
	int low, int high
	int items[];
	
	mergesort(sorted, low, high);

return 0;
}

void mergesort(int A[], int low, int high)
{

        if(low < high)
	{
        mid := (low+high)/2;
        mergesort(items, low, mid);
        mergesort(items, mid, high);
	merge(items, low, high, mid);
	}
	else ;


}































